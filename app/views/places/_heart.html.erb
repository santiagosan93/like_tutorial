<i
  class="<%= current_user.liked?(place) ? 'fas' : 'far' %> fa-heart heart"
  data-place-id="<%= place.id %>"
  data-action="click->like#updateLike"
  data-method="<%= current_user.liked?(place) ? 'delete' : 'post' %>">
</i>
